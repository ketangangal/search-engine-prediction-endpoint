# Enable Bucket Versioning
# As soon as we get bucket event trigger -> pull the image into the system
# Load Model From Models

# Prediction Flow
# Enable Bucket Versioning : As data is received by the bucket trigger lambda
